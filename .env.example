# Base URL of the cheevo NestJS backend (server-side only — never exposed to the browser).
BACKEND_URL=http://127.0.0.1:3000

# App-store listing URLs for the marketing store buttons. Empty until the apps are live.
NEXT_PUBLIC_APP_STORE_URL=
NEXT_PUBLIC_PLAY_STORE_URL=
